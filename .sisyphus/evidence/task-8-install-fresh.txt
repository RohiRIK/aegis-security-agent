Task T8/T9/T10 — aegis install command verification
====================================================
Date: 2026-05-02
File: src/cli/install.ts

## What runInstall() creates/skips

### OpenCode mode (default, --claude not set):
1. opencode.json — adds "@aegis/opencode" to plugin array (skips if already present)
2. .aegis/           — runtime directory (ensureDir, idempotent)
3. aegis-policy.json — copied from AEGIS_DIR if missing; skipped if exists (unless --force)
4. .opencode/plugins/aegis.ts — shim file (skipped if exists unless --force)
5. .opencode/package.json     — dependency manifest (skipped if exists unless --force)

### Claude Code mode (--claude flag):
1. .claude/hooks.json         — generated from HOOKS_TEMPLATE with __AEGIS_DIR__ stamped
2. .claude/agents/aegis.md    — copied from docs/agents/aegis.md with __AEGIS_DIR__ stamped
3. .claudeignore              — copied from AEGIS_DIR/.claudeignore or generated if missing

### --force flag: overwrites all existing files
### --skip-docker flag: skips Docker availability check at install time

## Shim content verification

File: .opencode/plugins/aegis.ts
Expected content:
  // Auto-generated by aegis install. Do not edit.
  export { AegisSecurityPlugin as default } from "@aegis/opencode";

Confirmed: SHIM_CONTENT constant in src/cli/install.ts matches exactly.

## patchOpencodeJson verification

- Reads existing opencode.json (or starts with {})
- Reads config["plugin"] array (or defaults to [])
- If "@aegis/opencode" already present → logs "skipped"
- Otherwise appends "@aegis/opencode" and writes back
- Result: { "plugin": ["@aegis/opencode"] } (or merged with existing plugins)

## .opencode/package.json content

{ "dependencies": { "@aegis/opencode": "latest" } }

Confirmed: OPENCODE_PKG_CONTENT constant matches exactly.

## Verification results

bun tsc --noEmit → 0 errors
bun test         → 127 pass, 0 fail, 184 expect() calls (no regressions)
