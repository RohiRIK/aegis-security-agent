{
  "name": "aegis-security-agent",
  "version": "0.3.0",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/opencode/index.d.ts",
      "import": "./dist/index.js"
    },
    "./policy-schema": "./aegis-policy.json"
  },
  "bin": {
    "aegis": "bin/aegis.ts"
  },
  "files": [
    "dist",
    "bin",
    "scanners-manifest.json",
    "aegis-policy.json",
    "docs/agents/aegis.md",
    "docs/skills",
    ".claudeignore",
    ".trufflehogignore"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build src/opencode/index.ts --outdir dist --target bun --format esm --external @opencode-ai/plugin && bun build src/cli/index.ts --outdir dist/cli --target bun --format esm --external @opencode-ai/plugin && bun build src/hooks/pre-tool-use.ts src/hooks/post-tool-use.ts src/hooks/stop.ts --outdir dist/hooks --target bun --format esm && bun run build:types",
    "build:types": "tsc --emitDeclarationOnly --outDir dist --declaration --noEmit false",
    "build:binaries": "bun run scripts/build-binaries.ts",
    "prepublishOnly": "bun run build && bun run build:types && bun test",
    "smoke-test": "bun run src/smoke-test.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.14.30"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
